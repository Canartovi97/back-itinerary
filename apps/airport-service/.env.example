PORT=3001
API_COLOMBIA_BASE_URL=https://api-colombia.com/api/v1
CIRCUIT_BREAKER_FAILURE_THRESHOLD=3
CIRCUIT_BREAKER_RESET_TIMEOUT_MS=30000
# Must match itinerary-service's JWT_SECRET. Not enforced on any current
# endpoint (see infrastructure/security/jwt-auth.guard.ts) but ready for
# when a protected one is added.
JWT_SECRET=dev-secret-change-me
